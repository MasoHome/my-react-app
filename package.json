{
  "name": "my-react-app-root",
  "private": true,
  "workspaces": [
    "aclient",
    "aserver"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=aclient\" \"npm run dev --workspace=aserver\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}